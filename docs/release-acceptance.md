# W_SHA 实机发布验收表

这份清单用于阶段 7 的最终验收。`corepack pnpm check:all`、Playwright 移动设备仿真和 `corepack pnpm verify:lan` 只能作为前置证据，不能替代真实 Windows、Android、iPhone 或微信浏览器测试。

## 0. 自动化前置证据（非实机验收）

- 日期：2026-08-01
- `corepack pnpm check:all`：通过；shared 35、server 214、web 34，Playwright 7/7。
- `corepack pnpm quality`：通过；生产依赖高危审计无已知漏洞，仓库契约检查通过。
- `corepack pnpm verify:lan`（默认固定端口 `35173`）：通过；LAN HTTP、来源白名单、跨 Socket 生命周期重放、接管批准凭证、快照重启恢复共 10 项检查。
- 便携版 SHA256：`5C19864C04A5D06B7C3F9A2F96173F7CCB47B59D33E5411EA2E9F2BDF574B20E`
- 安装包 SHA256：`9EA6C93456A8002772F41EF6FF87763C1BCBBA08C10ABBE7BB9526B9CF900BFF`
- 当前 LAN 地址：`192.168.3.70`

以上记录只证明本机自动化和生产便携版烟测，不代表真实设备验收通过。

## 1. 验收信息

- 验收日期：
- Windows 主机：
- Windows 版本：
- 主机 WLAN IPv4：
- 安装包版本：`0.1.0`
- 安装包 SHA256：
- 路由器/局域网名称：

## 2. Windows 安装与防火墙

- [ ] 以管理员身份运行 `release/W_SHA-Setup-0.1.0.exe`。
- [ ] 安装完成后能从开始菜单或桌面快捷方式启动。
- [ ] 安装目录包含 `node.exe`、`app/server/dist/index.js` 和 `app/public/index.html`。
- [ ] 服务监听 `TCP/35173`，主机页面自动打开。
- [ ] 安装后检查规则只允许本地子网访问：

```powershell
Get-NetFirewallRule -DisplayName "W_SHA 局域网狼人杀" |
  Get-NetFirewallPortFilter
Get-NetFirewallRule -DisplayName "W_SHA 局域网狼人杀" |
  Get-NetFirewallAddressFilter
```

- [ ] 规则覆盖当前网络配置，但没有把端口开放给任意远程地址。
- [ ] 卸载后服务进程停止，`W_SHA 局域网狼人杀` 防火墙规则被删除，游戏数据仍按产品说明保留。

## 3. 二维码与浏览器加入

| 客户端 | 扫码/打开链接 | 输入昵称加入 | 刷新后恢复 | 断网后重连 | 结果/备注 |
| --- | --- | --- | --- | --- | --- |
| Android Chrome | [ ] | [ ] | [ ] | [ ] | |
| iPhone Safari | [ ] | [ ] | [ ] | [ ] | |
| 微信浏览器 | [ ] | [ ] | [ ] | [ ] | |

验收时确认：

- [ ] 手机和主机处于同一允许设备互访的 WLAN。
- [ ] 二维码公布的是主机 LAN 地址，不是 `localhost` 或 `127.0.0.1`。
- [ ] 主机页面不泄露玩家身份或夜间秘密。
- [ ] 玩家页面只显示该玩家有权查看的信息。

## 4. 异常恢复

- [ ] 手机锁屏 10 秒后解锁，玩家身份和当前阶段保持不变。
- [ ] 手机切到后台 30 秒后返回，页面恢复且不会重复加入。
- [ ] 手机短暂断开 WLAN 后恢复，玩家能在重连宽限期内回到原座位。
- [ ] 浏览器刷新后恢复原玩家会话。
- [ ] 主机服务重启后，房间、玩家、当前阶段和可用重连凭证恢复。
- [ ] 重启过程中不会产生重复玩家或重复聊天消息。

## 5. 完整对局

- [ ] 从创建房间、扫码加入、身份确认开始。
- [ ] 完成至少一轮夜间行动、白天发言和投票。
- [ ] 正确公布胜负并显示对局记录。
- [ ] 返回大厅后玩家座位和新一局配置可正常使用。
- [ ] 记录一张主机公共屏幕和一张玩家页面截图（遮盖房间号、二维码 token 等敏感信息）。

## 6. 结论

- 通过项：
- 失败项：
- 是否需要修复：
- 复测日期：
- 验收人：
